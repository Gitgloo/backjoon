#include <iostream>

using namespace std;

int main(void) {
	cin.tie(NULL);
	ios_base::sync_with_stdio(false);

	int T;

	cin >> T;
	while (T--) {
		int n, m;

		cin >> n >> m;
		while (m--) {
			int temp;

			cin >> temp >> temp;
		}

		cout << n - 1 << '\n';
	}
	return 0;
}